extract = 'total_dyn_inst: (\d+)'
benchmarks = './test/*/*.bril'

[runs.baseline]
pipeline = [
    "bril2json",
    "brili -p {args}"
]

[runs.lvndce]
pipeline = [
    "bril2json",
    "./brilc lvn-dce",
    "brili -p {args}"
]